---
cargo/nash-driver: minor
cargo/nash-codegen: minor
---

Rename `FromData.validateData` to `FromData.validate`. Update method definitions, imports, and calls to use `validate`; validation behavior is unchanged.
