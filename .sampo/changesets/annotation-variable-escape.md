---
cargo/nash-constrain: minor
cargo/nash-solve: patch
---

Report an annotation variable that is fixed by an outer scope as a type error,
with its definition name and region, instead of panicking during generalization.
