---
cargo/nash-can: minor
---

Retain impl metadata in module interfaces and expose global trait and impl tables to subsequent passes. Reject duplicate impl keys across build interfaces, with both defining modules in the diagnostic.
