---
cargo/nash-ast: minor
cargo/nash-can: minor
cargo/nash-constrain: patch
cargo/nash-driver: patch
---

Check value annotations, including nested lets, while preserving original alias kind contracts. Include kind schemes and bounds in interface fingerprints, version serialized interfaces, and treat old cache formats as misses.
