module Main exposing (..)

import Builtin exposing (..)

type alias count = int
type alias Alias 'a = 'a
type alias Record 'a = ({ field : ('a : Big) } : Big)
keep : Alias 'a -> Alias 'a
keep x = x
wrap : ('a : Big) -> Record 'a
wrap x = Record x
