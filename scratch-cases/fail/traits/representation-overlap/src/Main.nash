module Main exposing (..)
trait Pick 'a where
    pick : 'a -> 'a
impl Pick (list ('a : Big)) where
    pick x = x
impl Pick (list ('a : Const)) where
    pick x = x
big : list Data -> list Data
big = pick
little : list unit -> list unit
little = pick
