module Unit exposing ( Unit(..) )


import Literal exposing ( FromUnit )
import Lift exposing ( Lift )


{-| A Data-represented unit value, encoded as constructor zero without fields. -}
type Unit
    = Unit


impl Lift unit Unit where
    lift _ = Unit.Unit

    lower _ = ()


impl FromUnit Unit where
    fromUnit _ = Unit
